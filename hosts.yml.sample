all:
  hosts:
    us: 
      ansible_host: avicontroller.corp.local
  vars:
    avi_credentials:
      controller: "{{ ansible_host }}"
      username: admin
      password: password
      api_version: 22.1.3
    upgrade_file_location: /tmp/avi_patch.pkg
    ansible_python_interpreter: "{{ ansible_playbook_python }}"

